/**
 * Re-export onboarding actions from the canonical feature location.
 * Domain logic lives in src/features/onboarding/actions.ts (R2.1).
 */
export { chooseRoleAction } from "@/features/onboarding/actions";
