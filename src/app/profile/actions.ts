/**
 * Re-export profile actions from the canonical feature location.
 * Domain logic lives in src/features/profile/actions.ts (R2.1).
 */
export { updateProfileAction } from "@/features/profile/actions";
