/**
 * Re-export verification actions from the canonical feature location.
 * Domain logic lives in src/features/auth/actions.ts (R2.1).
 */
export { resendEmailVerificationAction } from "@/features/auth/actions";
